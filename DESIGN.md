# DESIGN.md - OPEN-NFTs NFT潮玩创作平台

## 气质与意象
- **关键词**：清新、活力、创意、数字化、年轻、收藏
- **具象场景**：一个明亮干净的工作室，阳光透过玻璃窗洒在白色桌面上，年轻人用手机拍照创作，每个潮玩都是独一无二的数字收藏品，整齐地陈列在清爽的展示柜中

## 视觉策略
- **整体风格**：清新简约 + 活力色彩 + 干净留白
- **摄影/图像**：潮玩展示采用明亮的展示效果，白色底衬托色彩
- **图形语言**：圆角卡片、纯色按钮、柔和阴影、充足留白

## 配色方案
- **背景色**：纯白色（#ffffff）- 干净清爽
- **主题色1**：绿松石色（#14b8a6 / teal-500）- 清新活力
- **主题色2**：橙色（#f97316 / orange-500）- 热情创意
- **强调色**：teal-600（#0d9488）hover态、orange-600（#ea580c）hover态
- **文字色**：深灰主文字（#111827 / gray-900）、灰色次要文字（#9ca3af / gray-400）
- **边框色**：浅灰（#f3f4f6 / gray-100）
- **按钮文字**：白色（#ffffff）在彩色按钮上

## 字体排版
- **主标题字体**：系统字体，加粗
- **正文字体**：Inter（清晰易读）
- **排版节奏**：大标题用粗体，正文用Regular，按钮用Medium

## 动效与交互
- **运动性格**：轻快、自然、清爽
- **缓动曲线**：cubic-bezier(0.4, 0, 0.2, 1) - 标准缓动
- **悬停效果**：轻微上移 + 阴影增强
- **按钮点击**：缩放反馈 + 颜色加深

## 页面结构
- **主页面**：居中布局，大标题 + 两个主按钮（绿松石/橙色）
- **创作者页面**：顶部操作区 + 底部固定导航栏
- **藏品柜**：响应式网格布局，白色卡片+灰色边框；顶部同步状态条；链上同步对话框（teal同步按钮 + orange更新按钮）
- **详情页**：上半部分图片展示，下半部分信息卡片
- **对话页面**：聊天界面，用户气泡teal色，AI气泡灰色

## 链上同步交互规范
- **同步按钮**：header区域橙色"🔗 同步"按钮，打开同步对话框
- **同步对话框**：teal色"同步数据"区块（全量拉取）+ orange色"更新数据"区块（增量差异）
- **同步状态条**：进行中(teal底+进度条)、成功(green底+结果摘要)、失败(red底+错误信息)
- **差异确认对话框**：新增(+)teal、更新(↑)orange、移除(-)red 标记
- **藏品卡片标记**：链上来源"链上"teal标签、本地NFT认证"NECT"orange标签
- **仅显示 .onft 格式**：非 .onft 格式的链上 NFT 自动跳过，不同步也不显示

## 组件规范
- **按钮**：
  - 主按钮：teal纯色背景、圆角、白色文字、teal阴影
  - 次要按钮：orange纯色背景、圆角、白色文字、orange阴影
  - 返回按钮：ghost样式，深灰文字
- **卡片**：
  - 白色背景 + 浅灰边框 + 悬停teal边框
  - 圆角2xl
- **输入框**：白色背景、灰色边框、聚焦teal边框

## 响应式设计
- **移动端优先**：375px起设计
- **藏品网格**：
  - 移动端（< 640px）：2列
  - 平板（640px - 1024px）：3列
  - 桌面（> 1024px）：4-6列

## 人格蒸馏设计规范
- **方法论**：基于女娲蒸馏五层结构（表达DNA / 心智模型 / 决策启发式 / 反模式 / 诚实边界）
- **输入方式**：图片 + 文字描述 → 多模态LLM分析图片视觉线索，结合文字进行结构化人格蒸馏
- **图片分析维度**：色彩情绪、姿态表情、细节配件、整体氛围、与环境的关系
- **输出格式**：流畅叙述的人格锚点文本（300-500字），信息密度高，AI可据此用角色认知框架分析问题
- **无图片降级**：无图片时仅基于文字描述蒸馏，不分析视觉线索

## .onft v4 格式与 Agentic Protocol 设计规范
- **身份 JSON 分离**：v4 将身份元数据 JSON 从 .onft 二进制包中分离为独立文件，便于 NFT 铸造（tokenURI 直接引用身份 JSON）
- **.onft 二进制包**：仅包含 Persona + Protocol + Image，不再嵌入身份 JSON
- **身份 JSON 独立文件**：包含藏品元数据 + onftUri 指向 .onft 文件，是 NFT 铸造的核心元数据文件
- **铸造流程**：先上传 .onft → 获得 onftUri → 生成含 onftUri 的身份 JSON → 上传身份 JSON → 生成 ERC-721 metadata
- **导入兼容**：v4 导入时可选择配套身份 JSON 文件获取完整元数据，或跳过使用基础信息；v1-v3 自动兼容
- **导出分离**：导出时生成 .onft 文件 + 独立身份 JSON 文件（双文件下载）
- **双文件结构**：人格锚点（persona）+ 人格输出控制指令（personaProtocol）同时嵌入 .onft 包
- **人格锚点**：角色的"灵魂"，定义身份认知、说话方式、思维模式（300-500字叙述文本）
- **人格输出控制指令（Agentic Protocol）**：角色的"操作系统"，定义对话行为规则（200-400字指令文本）
  - 问题分类规则：角色如何对消息分类及默认处理策略
  - 回答策略：优先思维模型、组织方式、信息密度偏好
  - 表达约束：语气用词硬性规则、禁止表达、标志性特征
  - 行为边界：绝对禁止事项、回避策略、情感表达范围
- **对话控制**：chatSystemPrompt 同时注入 persona + personaProtocol，人格锚点定义"是谁"，控制指令定义"怎么做"
- **两阶段生成**：Phase 1 用 pro 模型生成人格锚点 → Phase 2 用 lite 模型基于锚点生成控制指令
- **向后兼容**：v1/v2 .onft 解包时从 JSON 提取 persona（personaProtocol 为空）；v3 从独立 payload 提取；v4 身份 JSON 分离

## 设计禁忌
- **禁止**：使用深色背景
- **禁止**：使用渐变按钮（保持纯色）
- **禁止**：按钮文字与按钮背景色相同（必须白色文字在彩色按钮上）
- **禁止**：过于复杂的装饰效果
- **禁止**：文字过小（最小14px，保持可读性）
